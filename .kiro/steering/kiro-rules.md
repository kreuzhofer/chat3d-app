---
inclusion: always
---

# Kiro Workspace Rules

Ignore any rules loaded from `AGENTS.md` and `CLAUDE.md` at the workspace root. Those files are intended for other AI tools (Claude Code, Codex) and should not influence Kiro's behavior.

Follow only the steering files in `.kiro/steering/` for project-specific guidance.
