fastapi[standard]==0.129.0
pydantic==2.12.5
# build123d from dev branch has native aarch64 (ARM) Linux support:
# - uses cadquery-ocp-novtk>=7.9 (has aarch64 wheels)
# - uses py-lib3mf on aarch64 (has aarch64 wheels)
# - uses ocpsvg>=0.6 and ocp_gordon>=0.2 (compatible with OCP 7.9.x)
# See: https://github.com/gumyr/build123d/pull/1236
# Pinned to the last verified dev commit: later dev HEAD removed ShapePredicate,
# which gridfinity_build123d still imports. Bump both pins together after
# verifying the pairing (SpurGear + gridfinity imports + a render).
build123d @ git+https://github.com/gumyr/build123d.git@e8cae0660
# bd_warehouse: parametric mechanical components (threads, fasteners, bearings, gears, pipes)
# Pin to 0.2.0 — fixes TreeError with anytree 2.13 and ShapeList.wrapped API change
bd_warehouse>=0.2.0
# gridfinity_build123d is installed in the Dockerfile with --no-deps: its
# pyproject pins bd_warehouse to a git commit, which pip cannot reconcile
# with bd_warehouse>=0.2.0 above (ResolutionImpossible).
# typing_extensions backports typing.override for Python <3.12
typing_extensions>=4.0
# pyrender-based screenshot rendering (EGL offscreen)
trimesh>=4.0
pyrender>=0.1.45
pyglet<2
Pillow>=10.0
PyOpenGL>=3.1
